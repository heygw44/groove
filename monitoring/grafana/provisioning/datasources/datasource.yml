# Grafana 데이터소스 자동 프로비저닝. Prometheus 를 기본 소스로 등록한다.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus
    access: proxy
    # compose groove-net 내부 주소.
    url: http://prometheus:9090
    isDefault: true
    editable: false
