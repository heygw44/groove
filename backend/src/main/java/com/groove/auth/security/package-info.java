/**
 * Spring Security 베이스라인.
 *
 * <p>Stateless API용 SecurityFilterChain, CORS, 인증 실패 응답(ProblemDetail) 구성.
 * JWT 인증 필터는 W4(#W4-2)에서 연결되며, 본 패키지는 자리(slot)만 마련한다.
 */
package com.groove.auth.security;
