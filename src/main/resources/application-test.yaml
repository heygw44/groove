spring:
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        show_sql: true

# 테스트용 CORS (와일드카드 + credentials)
cors:
  allowed-origin-patterns: ["*"]
  allow-credentials: true

logging:
  level:
    root: WARN
    com.groove: DEBUG
