spring:
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        show_sql: true

# 테스트용 CORS (와일드카드 + credentials)
cors:
  allowed-origin-patterns: ["*"]
  allow-credentials: true

logging:
  level:
    root: WARN
    com.groove: DEBUG
